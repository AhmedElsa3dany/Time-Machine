# Time Machine

A Flutter app that lets users explore visual journeys through different eras of time.

## Getting started

Install Flutter, then run:

```bash
flutter pub get
flutter run
```

The app is built with Flutter and uses bundled time-machine video assets.
